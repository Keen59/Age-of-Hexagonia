using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Global_Kapat : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }
    public void closser()
    {
        gameObject.SetActive(false);
    }
    // Update is called once per frame
    void Update()
    {
        
    }
}
